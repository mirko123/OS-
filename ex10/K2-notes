Какво представлява /etc/passwd - user:password:...:home_dir:shell
ls -l -> grep "^l";
-d, -e, ... - проверките се отнасят за това което сочи връзката
test  -e $link_file && echo 1
readlink

owner
права -> те са за собственика на файла !!! -> test [ -r $file ] -> четене за процеса (за собственика на процеса)
$HOME -> за текущия потребител -> .bashrc за всеки потребител ...

ls -i -> inode number
ls -l| cut -d' ' -f2 -> hardlinks count (за файл) или брой на поддиректории (включително . и ..)

Какво е структура от данни ?
Проверка на входа ...

for i in `who` -> приема space за разделител ...
cat <<< 'str as stdin'  ===> for read line;do echo $line; done <<< $(ls -l)
cat <(ls)

използвайте смислени имена !!!
сегменти които са верни

Разлика между - PID, UID, USER
